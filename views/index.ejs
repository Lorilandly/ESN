<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="/stylesheets/index.css" rel="stylesheet" type="text/css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
        <link rel="stylesheet" href="/bootstrap-icons/bootstrap-icons.css">
    </head>
    <body>
        <div class="main-container">
            <div class="header">
                <h1 class="header-text">Emergency Social Network</h1>
            </div>
            <% if (!isAuthenticated) { %>
                <!-- Show this when not authenticated -->
                <form action="/join" class="main-form" method="GET">
                    <button class="form-button">Join</button>
                </form>
            <% } else { %>
                <!-- Show this when authenticated -->
                <!-- Display locals.users -->
                <div class="user-list">
                    <div class="user-list-header">
                        <div class="user-list-header-name">Name</div>
                        <div class="user-list-header-status">Status</div>
                    </div>
                    <script src="/socket.io/socket.io.js"></script>
                    <script src="/javascripts/status.js"></script>
<<<<<<< HEAD
                    <div class="user-list-body" id="user-list-body">
                    </div>
=======
                    <% if (users.length) { %>
                        <div class="user-list-body">
                            <% users.forEach(function(user) { %>
                                    <div class="user-list-body-element">
                                        <div class="user-list-body-element-name"><%= user.username %></div>
                                        <% if (user.current_status === 'ONLINE') { %>
                                            <div class="user-list-body-element-status-online" id="user-status-<%= user.username %>"><%= user.current_status %></div>
                                        <% } else { %>
                                            <div class="user-list-body-element-status-offline" id="user-status-<%= user.username %>"><%= user.current_status %></div>
                                        <% } %>
                                        <!-- <div class="user-list-body-element-status" id="user-status-<%= user.username %>">Status</div> -->
                                    </div>
                            <% }); %>
                        </div>
                    <% } %>
>>>>>>> main
                </div>
                <div class="footer">
                    <div class="footer-element">
                        <button class="action-button">
                            <i class="bi bi-people text-primary"></i>
                        </button>
                    </div>
                    <div class="footer-element">
                        <form action="/publicWall" method="GET">
                            <button class="action-button">
                                <i class="bi bi-chat-dots"></i>
                            </button>
                        </form>
                    </div>
                    <div class="footer-element">
                        <button class="action-button">
                            <i class="bi bi-chat-heart"></i>
                        </button>
                    </div>
                    <div class="footer-element">
                        <form action="/logout" class="main-form" method="GET">
                            <button class="action-button">
                                <i class="bi bi-box-arrow-right"></i>
                            </button>
                        </form>
                    </div>
                </div>
            <% } %>
        </div>
        <script src="/javascripts/users.js"></script>
    </body>
</html>